const IP = 'localhost';
const PORT = 50541;
const upkey = "Move: up";
const downkey = "Move: down";
const leftkey = "Move: left";
const rightkey = "Move: right";


module.exports = {
  IP,
  PORT,
  upkey,
  downkey,
  leftkey,
  rightkey,
};